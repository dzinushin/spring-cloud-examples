#logging:
#  level:
#    root: debug
spring:
  application:
    name: spring-gateway-demo
  cloud:
    gateway:
      routes:
        - id: route2
          uri: http://httpbin.org
          predicates:
            - Path=/post
